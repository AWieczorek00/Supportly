import {Order} from '../order/Order';
import {Employee} from '../employee/Employee';

export interface TaskCriteria{
  name:string
  companyName:string;
  firstName:string;
  lastName:string;
  done:boolean;

}
