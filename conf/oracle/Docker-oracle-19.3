FROM doctorkirk/oracle-19c

# Konfiguracja timezone dla Debian/Ubuntu
RUN apt-get update && apt-get install -y tzdata
ENV TZ=Europe/Warsaw
RUN ln -snf /usr/share/zoneinfo/$TZ /etc/localtime && echo $TZ > /etc/timezone